# v0.2 改造清单

> **v0.2.1 补丁（导入超时修复）**
>
> 采购记录导入时前端一直转圈。原因是 commit 接口逐行 await：
> 每行先查/建物料、再查/建供应商、再插入，1,710 行 ≈ 2,700 次数据库往返，
> 跨区域每次 30~50ms，合计 1~2 分钟，serverless 函数超时。
> **数据其实写进去了**（事务在服务端提交成功），只是浏览器等不到响应。
>
> 已改成集合操作：先一次性把型号/供应商/客户解析成 id 映射，再用 `unnest` 批量插入，
> 总共十来条 SQL，与行数基本无关。八条批量语句已逐条在生产库上跑通并回滚验证。
>
> 同时修掉一个会让下一步出错的坑：采购单引用的是 ERP 的**「编号」**（如 `紫诺翔科技`），
> 而供应商表的主字段是**公司全称**（`深圳市紫诺翔科技有限公司`），228 家里只有 4 家两者相同。
> 直接按全称导通讯录，会新建 1,015 家重复供应商、而已有的 228 家仍然没有联系方式。
> 现在通讯录 CSV 的「供应商名称」列改用编号，全称放进「公司全称」列。


改造围绕一条主线：**让数据能进得来、也出得去**。
所有数据库变更已直接应用到生产库（Supabase `actafgreputbsqaktbzo`），代码待推送。

---

## 一、数据库（已在生产库执行）

| 迁移 | 内容 | 状态 |
|---|---|---|
| `001_phase1_schema` | 客户表、别名表、合并日志、导入批次表；parts/shipments/supplier_parts/suppliers/boms 扩展；updated_at 触发器；统计刷新函数 | ✅ 已执行 |
| `002_dedup_parts` | 合并 630 组重复型号 | ✅ 已执行 |
| `004_merge_log_moved_ids` | 合并日志记录搬迁行 id，支持精确回滚 | ✅ 已执行 |
| `005_clean_main_pn` | 清掉主记录型号尾部的斜杠 | ✅ 已执行 |
| `006_pn_norm_trigger` | pn_norm / alias_norm / price_flag 全部由触发器自动维护 | ✅ 已执行 |

`003_rollback_merge.sql` 是出问题时手动执行的撤销脚本，不参与自动迁移。

### 清洗结果（生产库实测）

| 指标 | 改造前 | 改造后 |
|---|---|---|
| 物料记录 | 5,432（含 1,273 条重复） | 4,789 在库 + 643 条标记为已合并 |
| 重复型号组 | 630 组 | **0** |
| 成交历史被拆散 | 116 组 | **0** |
| 型号尾部带斜杠 | 665 | **0** |
| 零价出货记录 | 63 条混在均价里 | 63 条已标记，不参与统计 |
| 可搜索别名 | 无 | 643 条（旧型号照样搜得到） |

举个具体例子：`0603104KB` 原本被拆成两条，现在合并后 ship_count 113 次、均价 0.0083，
旧型号 `0603104KB/` 保留为别名。这类情况有 116 组，之前每一次询价都在赌命中哪一条。

**合并是可撤销的**：批次 `MERGE-20260803-122550` 记在 `part_merge_log`。
首批因为没记搬迁行 id，精确回滚需要 Supabase PITR；之后从页面触发的合并都可一键撤销。

---

## 二、新增功能

### 数据导入（`/import`）— 最关键的一块
之前所有数据靠一次性 seed 脚本，上线后没有任何入口能新增出货、更新报价、修改物料。

- 支持 5 类数据：供应商报价 / 出货流水 / 物料主数据 / 供应商档案 / 客户档案
- CSV、TSV，自动识别中文表头（"单价""报价""含税单价"都能映射到 price）
- 自动清洗：`2026/5/28` → 日期，`2,000` / `￥23.50` → 数字，`0402499KF/ ` → 归一化型号
- 四步向导：选文件 → 字段映射（可手动改）→ 校验预览 → 确认入库
- **整批可回滚**，包括这批自动建档出来的物料和供应商

### 客户管理（`/customers`）
`shipments` 之前没有客户字段，"这个客户买过什么"根本答不了。现在字段已就位，
导入带客户列的出货流水即可生效。

### 询价记录（`/inquiries`）
每次 AI 询价的结果之前只写进 `boms` 表，前端无处可看。现在有单号、金额、毛利、
成交/流失标记，工作台的转化率就是从这里来的。

### 数据体检（`/data-health`）
重复型号扫描与一键合并、异常价格清单、完整度看板、合并批次撤销。
数字全部是生产库实时扫描，不是估算。

---

## 三、性能与正确性修复

| 问题 | 之前 | 现在 |
|---|---|---|
| 批量匹配 | 每个型号 4 次 SQL，200 行 BOM ≈ 800 次往返（连接池 max 很小，全部排队） | 整批固定 5 次查询，与行数无关 |
| 物料搜索 | LATERAL 对全表 5,432 行现算出货聚合，然后才 LIMIT | 读 parts 上的冗余统计字段 |
| 分页 | 没有，只能看前 200 条 | 真分页 + 总数 |
| 模糊匹配 | `ILIKE '%x%'` + 按型号长度排序，trigram 索引建了没用上 | `similarity()` 相似度排序 |
| 均价计算 | `avg(unit_price)` 未过滤零值，报价被系统性拉低 | 只统计 `price_flag='ok'` |
| 成本口径 | 只看目录成本 | 有效供应商报价优先，回退目录成本 |
| 供应商评分 | 一个品牌下所有物料价格混在一起算波动率，实测普遍 >1，等于 40% 权重全废 | 按单个型号在时间上的变异系数加权，实测 0.007~0.16 |
| 评分写入 | 218 个品牌循环 UPDATE | 一条 SQL 批量更新 |
| 询价存档 | 只塞进 `parsed_parts` 大 JSON | 同时写 `quote_line_items`，可用标准 SQL 统计 |

评分修正后的实际变化：UNIOHM 60.0 → 94.4，YAGEO 54.4 → 91.0，
之前所有品牌的"价格稳定性"都是 0 分。

---

## 四、验证情况

由于沙箱环境无法访问 npm registry，`next build` 没跑起来。已完成的验证：

- ✅ 21 个 `.ts` 文件全部通过 Node 类型剥离加载（catch 到 2 处真实 bug：
  `ImportKind` / `PreviewResult` 被当作值导入，在 `isolatedModules` 下会运行时报错，已改为 `import type`）
- ✅ 13 个 `.tsx` 文件 JSX 标签配对检查通过
- ✅ 所有 import 路径与导出名校验通过；无未使用导入
- ✅ 每一条 SQL 都在生产库上实际跑过
- ✅ 导入解析逻辑单元验证：CSV/TSV、引号、中文表头、日期与数字清洗、脏行拒绝
- ✅ **端到端演练**：模拟导入供应商报价 + 出货 + 新客户 → 校验生效 → 整批回滚 →
  数据库回到完全一致的状态（supplier_parts 0、customers 0、ship_count 恢复 113、shipments 5974）

**还需要你在本地跑一次 `npm run build` 确认**，这是唯一没覆盖到的环节。

---

## 四点五、2025 年 ERP 数据接入（新增）

三份 ERP 导出文件其实是 **Excel 5.0/95 格式 + GBK 编码**，
pandas / openpyxl / Excel 的新版解析器都读不了，所以仓库里自带了一个解析器
`lib/xls.js`（约 200 行，只做「读出单元格」这一件事）。

另一个坑：这三份都是**分组报表** —— 同一个客户/供应商只在该组第一行打印名字，
其余行是空的。销售表 1,505 行里只有 24 行有客户名（98% 空），
采购表 1,710 行里只有 229 行有供应商名。不做向下填充，数据基本等于废的。

| 文件 | 内容 | 去向 |
|---|---|---|
| 25年销售记录.xls | 1,505 行，24 个客户，1,171 行带成本 | 回填已有 shipments 的客户与成本 |
| 25年采购记录.xls | 1,710 行，228 家供应商，655 个型号 | → `supplier_parts` 918 条（每对取最近一次采购价） |
| 供应商表格.xls | 1,015 家，922 家有电话，748 家有联系人 | → 供应商联系方式 |

### 已完成
- ✅ 24 个客户已建档（生产库）
- ✅ `lib/xls.js` 解析器 + `scripts/xls2csv.js` 转换工具
- ✅ 导入模块新增「**采购记录**」类型 —— 实际付过的钱比口头报价可信
- ✅ 三份文件已转成 UTF-8 CSV，放在你的文件夹里
- ✅ 实测：ERP 原始列名（供应商号/单价/订单数量/货币/封装）**全部自动识别**，不用手工映射

### 销售回填的特殊处理
数据库里 2025 年已有 1,502 条出货，和这份 ERP 导出**同源**，直接再导会变成重复记录。
所以写了 `scripts/backfill-sales.js` 做匹配回填，三轮逐级放宽：
日期+型号+数量+单价 → 日期+型号+数量 → 日期+型号（唯一时）。

**30 行全年抽样实测：25 条命中最严格的键，5 条命中次严格，0 条未匹配。**

---

## 五、推送步骤

沙箱在 `git add` 时留下了一个锁文件，需要先删掉：

```bash
cd ~/Desktop/files/qijin-mvp
rm -f .git/index.lock
npm install
npm run build                  # ← 关键：确认构建通过再推
git add -A
git commit -m "v0.2: 数据导入、客户实体、询价追踪、数据体检、ERP 数据接入"
git push
```

数据库结构迁移**已经跑过了**，不需要再执行。如果以后要在新库上从零搭：`npm run seed`。

### 然后把 2025 年数据灌进去（三步，约 5 分钟）

需要本地有 `.env`，里面填 `DATABASE_URL`（跟 Vercel 上那份一样）：

```bash
# 1) 销售回填：先预演，看匹配率
node scripts/backfill-sales.js ~/Desktop/files/25年销售记录.xls
#    确认没问题再真跑
node scripts/backfill-sales.js ~/Desktop/files/25年销售记录.xls --commit
```

2) 打开「数据导入」页 → 选「**采购记录**」→ 拖入 `采购记录2025_待导入.csv` → 确认入库
   （918 条供应商报价，比价功能就活了）

3) 同一页 → 选「**供应商档案**」→ 拖入 `供应商通讯录_待导入.csv` → 确认入库
   （1,015 家联系方式，目前库里只有 9 家有联系人）

第 2、3 步走 UI 是故意的 —— 顺便验证导入模块在真实数据上能不能扛住，
而且万一有问题可以整批撤销。

> 以后 ERP 再导出新的 .xls，用 `node scripts/xls2csv.js 文件.xls --ffill 0` 转一下就能导。
> `--ffill 0` 是把第 0 列向下填充，分组报表必须加。

---

## 六、仍然没做的事

- 2023/2024 年的出货记录（3,350 条）仍然没有客户归属 —— 需要那两年的 ERP 销售导出
- 采购数据只有 2025 年，比价覆盖 655 个型号 / 全库 4,789 个（13.7%）
- `ACCESS_PASSWORD` 未设置，线上接口对公网开放（按你的意见本轮未处理）
- 无自动化测试、无 CI、无错误监控
- OCR、供应商阶梯报价、统一请求校验（zod）

如果 ERP 里能导出 2023、2024 两年的销售记录，同一个脚本可以直接跑，
把客户维度补齐到全部 5,974 条 —— 那时候客户分析才算真正完整。
