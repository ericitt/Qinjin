# 中国大陆访问问题

**结论：现在这套线上地址，大陆同事打不开。**

---

## 三个组件分别什么情况

| 组件 | 大陆可达性 | 说明 |
|---|---|---|
| `qijin-mvp.vercel.app` | ❌ 打不开 | `*.vercel.app` 被 DNS 污染，443 端口的 SNI 也被阻断 |
| Supabase（数据库） | ❌ 直连打不开 | API 和官网都被墙 |
| Claude API | ❌ 直连打不开 | Anthropic 不对大陆提供服务 |

看起来三个全废，但**实际上只有第一个是真问题**。

## 关键：只有浏览器那一跳需要能通

这个应用的数据库查询和 Claude 调用都发生在**服务端**：

```
浏览器 ──①──> Next.js 服务器 ──②──> Supabase（法兰克福）
                              └──③──> Claude API
```

② 和 ③ 是服务器到服务器，只要服务器本身在墙外，走的就是正常国际线路，不受影响。
**唯一被墙卡住的是 ①**——大陆浏览器连不上 Vercel。

也就是说：**不需要迁移数据库，也不需要换掉 Claude，只需要换一个大陆能连上的"前门"。**

---

## 三个方案

### 方案 A：绑自定义域名（先试这个，10 分钟）
`*.vercel.app` 这个域名本身被污染，换成自己的域名能绕开 DNS 污染这一层。

Vercel → Settings → Domains → 添加域名，按提示改 DNS。

- 成本：一个域名的钱
- 风险：Vercel 的 IP 段在大陆经常被限速，**能不能通、稳不稳定要实测**。
  有时候能开但很慢，有时候完全不行。
- 值得先试，因为几乎没成本

### 方案 B：香港服务器自建（推荐，可靠）
把 Next.js 部署到香港的云主机（阿里云香港、腾讯云香港、Vultr 等）。
香港节点**不需要 ICP 备案**，大陆直连通常没问题。

- 数据库、Claude 全都不用动，继续用现在的
- 成本：大约每月 ¥50~150
- 部署方式：`npm run build && npm start`，前面挂 nginx；或者用 Docker
- 这是我建议的方向

### 方案 C：迁到大陆云（最快，但最麻烦）
阿里云/腾讯云大陆节点，速度最好，但是：
- 域名必须 **ICP 备案**，通常要几周
- 大陆服务器连不上 Supabase 和 Claude，**数据库和 AI 都得一起换**
  （数据库迁到阿里云 RDS，AI 换成 DeepSeek / 通义千问 / Kimi）
- 工作量最大，只有在大陆用户量变大、对速度要求高的时候才值得

---

## 另一件事：数据库离亚洲太远

Supabase 项目现在在 **eu-central-1（法兰克福）**。
从香港到法兰克福一个来回大约 250~300ms。

这也是为什么之前那版「每个型号 4 次查询」的写法必须改掉 ——
一张 200 行的 BOM 就是 800 次往返，在这个距离下等于几分钟。
现在改成整批 5 次查询之后影响小多了，但如果要长期给亚洲用户用，
**建议把 Supabase 项目迁到 ap-southeast-1（新加坡）**，延迟能降到 30~50ms。

迁移方式：新建一个新加坡项目 → `pg_dump` 导出 → 导入 → 改 `DATABASE_URL`。
数据量不大（不到 2 万行），几十分钟能搞定。

---

## 建议顺序

1. 先绑个自定义域名，让大陆同事实测能不能开（成本最低）
2. 开不了或者太慢 → 上香港服务器（方案 B）
3. 顺手把数据库迁到新加坡，延迟从 300ms 降到 50ms
4. 大陆用户真正多起来之后，再考虑备案 + 全面国产化（方案 C）
