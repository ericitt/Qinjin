# 复制这个文件为 .env，放在项目根目录。改完要重启服务才生效。
# 本系统完全跑在内网，不连任何云服务。

# ── 数据库（本机 PostgreSQL）────────────────────────────────
# 装 PostgreSQL 时设的 postgres 用户密码，填在 :后面
# 注意：本地库没开 SSL，连接串里千万不要加 ?sslmode=require
DATABASE_URL=postgresql://postgres:你的数据库密码@localhost:5432/qijin

# ── 访问密码 ────────────────────────────────────────────────
# 打开网页要输的密码。留空 = 不需要密码（只建议在完全可信的内网里这么做）
ACCESS_PASSWORD=换成一个别人猜不到的密码

# ── 自动导入（可选）─────────────────────────────────────────
# 把 ERP 导出的文件丢进这个文件夹，系统会自己识别并入库
SYNC_INBOX=C:\qijin\收件箱
SYNC_APP_URL=http://localhost:3000
SYNC_INTERVAL_MS=300000

# ── AI 询价助手（可选，默认关闭）────────────────────────────
# 不填就不会显示 AI 菜单。国内可用 DeepSeek：https://platform.deepseek.com
# DEEPSEEK_API_KEY=
